{
  "name": "@structurizr-presenter/cli",
  "version": "0.0.0",
  "description": "CLI for structurizr-presenter.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "structurizr-presenter": "./dist/cli.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@structurizr-presenter/core": "workspace:*",
    "@structurizr-presenter/runtime": "workspace:*"
  }
}
